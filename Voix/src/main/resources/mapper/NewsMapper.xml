<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Voix.Dao.NewsDao">
	<select id="selectNews_map">
		SELECT NWCODE, NWTITLE, NWCONTENT, NWIMG, NWBIGHIT, TO_CHAR(NWDATE,'YYYY-MM-DD') AS NWDATE  FROM NEWS ORDER BY NWDATE DESC
	</select>
	<select id="selectNewsInfo_map">
		SELECT NWCODE, NWTITLE, NWCONTENT, NWIMG, NWBIGHIT, TO_CHAR(NWDATE,'YYYY-MM-DD') AS NWDATE  FROM NEWS WHERE NWCODE = #{nwcode} ORDER BY NWDATE DESC
	</select>
</mapper>